# Báo cáo buổi 1: Các khái niệm nền tảng về AI trong phát triển phần mềm

Ngày: 04/08/2026

## Phần 1. Giải thích các khái niệm

### 1.1. AI Coding IDE / AI Coding Assistant

AI Coding Assistant là công cụ dùng mô hình ngôn ngữ lớn để hỗ trợ viết, sửa, giải thích code ngay trong môi trường lập trình quen thuộc. AI Coding IDE đi xa hơn một bậc: toàn bộ trình soạn thảo được thiết kế lại xoay quanh AI, chứ không phải AI được gắn thêm vào một IDE có sẵn. Cursor và Windsurf thuộc nhóm này. GitHub Copilot ban đầu chỉ là một extension gắn vào VS Code hoặc JetBrains, dù sau này cũng phát triển thêm các chế độ agent riêng.

Cách phân loại hữu ích hơn cả hiện nay là theo mức độ tự chủ, đi từ thấp lên cao:

- Autocomplete: gợi ý từng dòng hoặc từng đoạn code khi gõ, người dùng vẫn chủ động từng bước.
- Copilot / Chat: trả lời câu hỏi, sinh code theo yêu cầu trong một phiên hội thoại, nhưng người dùng vẫn phải đọc, dán, kiểm tra thủ công.
- Agentic: công cụ tự lập kế hoạch, tự đọc nhiều file, tự chạy lệnh, tự sửa lỗi qua nhiều bước liên tiếp mà không cần người dùng can thiệp ở từng bước. Claude Code, Cursor Agent Mode, GitHub Copilot Workspace là ví dụ.

Ranh giới giữa ba mức này không cứng nhắc, một công cụ có thể cho phép chuyển qua lại giữa autocomplete và agent tùy tình huống. Nhưng theo khảo sát Stack Overflow 2025, chế độ agent tự động vẫn là thiểu số: chỉ 30,9% lập trình viên dùng agent tính cả tần suất hàng ngày, hàng tuần, hàng tháng cộng lại, phần lớn vẫn thích autocomplete hoặc chat vì lo ngại độ chính xác và bảo mật dữ liệu [1].

### 1.2. Large Language Models (LLMs)

LLM là mô hình được huấn luyện trên khối lượng văn bản khổng lồ để dự đoán token tiếp theo trong một chuỗi. Với code, token có thể là từ khóa, tên biến, dấu ngoặc. Bản chất thống kê này giải thích vì sao LLM giỏi tái tạo các mẫu quen thuộc, ví dụ viết hàm CRUD, parse JSON, viết test theo khuôn có sẵn, nhưng lại yếu hơn ở logic nghiệp vụ đặc thù mà nó chưa từng thấy trong dữ liệu huấn luyện.

Các mô hình dùng cho coding hiện nay như GPT, Claude, Gemini đều được tinh chỉnh thêm bằng dữ liệu code và phản hồi con người để bám sát ý định người dùng hơn. Nhưng cần nhớ một điều cốt lõi: LLM không hiểu code theo nghĩa một lập trình viên hiểu, nó chỉ tính xác suất chuỗi token nào hợp lý nhất. Vì vậy nó có thể sinh ra code chạy được nhưng sai logic nghiệp vụ, hoặc tự tin khẳng định một hàm, một API không hề tồn tại. Hiện tượng này gọi là hallucination.

### 1.3. Prompt Engineering

Prompt Engineering là kỹ thuật soạn câu lệnh đầu vào để LLM trả về kết quả đúng ý hơn. Kỹ thuật thường gặp gồm nêu rõ vai trò và định dạng đầu ra mong muốn, đưa ví dụ mẫu, yêu cầu mô hình suy luận từng bước, hoặc chia một yêu cầu lớn thành nhiều bước nhỏ hơn.

Vấn đề của Prompt Engineering là nó chỉ xử lý một lượt hỏi đáp đơn lẻ. Với coding thực tế, một prompt hay không đủ nếu mô hình không có thông tin về cấu trúc dự án, quy ước đặt tên, hay các ràng buộc nghiệp vụ đang tồn tại trong codebase. Đây là lý do khái niệm tiếp theo ra đời.

### 1.4. Context Engineering

Context Engineering giải quyết một câu hỏi khác hẳn: không phải "viết câu lệnh thế nào" mà là "cần đưa những thông tin gì vào cửa sổ ngữ cảnh để mô hình làm đúng việc". Nó bao gồm việc chọn lọc file nào cần đọc, tóm tắt lịch sử hội thoại để không vượt giới hạn token, gắn thêm tài liệu nội bộ, schema database, kết quả tìm kiếm, hoặc output của một công cụ khác vào ngữ cảnh trước khi mô hình sinh câu trả lời.

Phân biệt rõ hai khái niệm: Prompt Engineering là nghệ thuật diễn đạt yêu cầu trong một lượt trao đổi. Context Engineering là kỹ thuật quản lý toàn bộ bộ nhớ làm việc của hệ thống AI qua nhiều bước, nhiều file, nhiều công cụ. Một agent coding tốt có thể dùng prompt rất đơn giản, nhưng đằng sau nó là cả một pipeline chọn lọc context: đọc file nào trước, bỏ qua file nào, nén lại phần nào để không tràn bộ nhớ. Trong thực tế vận hành các công cụ như Claude Code hay Cursor, phần lớn công sức kỹ thuật nằm ở context engineering chứ không phải ở việc gõ prompt cho hay.

### 1.5. Model Context Protocol (MCP)

MCP là một giao thức mở do Anthropic công bố vào tháng 11/2024, dùng để chuẩn hóa cách một mô hình AI kết nối với dữ liệu và công cụ bên ngoài, ví dụ hệ thống file cục bộ, database SQL, Slack, GitHub, hay các API nội bộ của doanh nghiệp [7]. Trước MCP, mỗi công cụ AI phải tự viết tích hợp riêng cho từng nguồn dữ liệu, dẫn đến bài toán N công cụ nhân M nguồn dữ liệu cần tích hợp. MCP đưa ra một chuẩn chung, tương tự vai trò của cổng USB-C cho phần cứng: một AI hỗ trợ MCP có thể nói chuyện với bất kỳ MCP server nào mà không cần viết lại code tích hợp riêng cho từng bên.

MCP hiện được quản lý bởi Agentic AI Foundation thuộc Linux Foundation, tức là đã vượt ra khỏi phạm vi sản phẩm riêng của Anthropic để trở thành chuẩn ngành được nhiều bên tham gia. Bản đặc tả cập nhật tháng 7/2026 bổ sung các cơ chế như multi round-trip request và siết chặt phần xác thực, phản ánh việc giao thức đang được điều chỉnh để đáp ứng nhu cầu bảo mật doanh nghiệp [8].

### 1.6. Agentic Workflow

Agentic Workflow là mô hình vận hành trong đó AI không chỉ trả lời một câu hỏi mà tự lập kế hoạch, thực thi nhiều bước, gọi công cụ, kiểm tra kết quả, và tự sửa nếu bước trước sai. Vòng lặp này lặp lại cho đến khi đạt mục tiêu hoặc hết ngân sách bước cho phép. Một vòng lặp agentic coding điển hình: đọc yêu cầu, đọc code liên quan, viết code, chạy test, đọc lỗi, sửa lại, chạy lại.

Khác với Copilot truyền thống vốn chỉ có một lượt hỏi một lượt trả lời, agentic workflow cho phép AI làm việc độc lập trong thời gian dài hơn, có thể vài phút đến vài giờ, trước khi cần con người can thiệp lại. Đây cũng chính là lý do kiểm soát và niềm tin trở thành vấn đề lớn: agent càng tự chủ, sai sót càng khó phát hiện sớm, và càng có khả năng lan rộng trước khi có ai đó review lại toàn bộ.

## Phần 2. Trả lời câu hỏi

### 2.1. AI đang thay đổi quy trình phát triển phần mềm như thế nào?

Theo báo cáo DORA 2025 của Google Cloud, 90% người làm công nghệ hiện dùng AI trong công việc, và hơn 80% tin rằng AI giúp họ tăng năng suất cá nhân [3]. Nhưng DORA cũng đưa ra một phát hiện đáng cân nhắc hơn nhiều: AI được mô tả như một bộ khuếch đại, nghĩa là nó làm cho đội nhóm vốn đã làm tốt, có quy trình rõ ràng, có hạ tầng nội bộ chất lượng, trở nên tốt hơn nữa. Ngược lại, với đội nhóm vốn đã lộn xộn, AI chỉ khuếch đại sự lộn xộn đó nhanh hơn [3]. Nói cách khác, AI không tự sửa quy trình xấu, nó tăng tốc bất kỳ quy trình nào đang tồn tại, tốt lẫn xấu.

Về mặt cụ thể, ba khâu bị ảnh hưởng rõ nhất là viết code, review code, và debug. Viết code lặp lại, boilerplate, viết test theo khuôn mẫu là nơi AI tiết kiệm thời gian rõ nhất. Review lại phát sinh vấn đề mới. 45,2% lập trình viên trong khảo sát Stack Overflow 2025 nói rằng debug code do AI sinh ra tốn thời gian hơn debug code tự viết [1]. Đây là chi phí ẩn ít được nhắc tới khi người ta chỉ nói AI giúp code nhanh hơn.

Bằng chứng phản biện rõ nhất đến từ nghiên cứu METR năm 2025. Đây là một thử nghiệm ngẫu nhiên có đối chứng, cùng phương pháp luận dùng trong thử nghiệm thuốc, với 16 lập trình viên có kinh nghiệm làm việc trên chính các repo mã nguồn mở họ đã quen thuộc trung bình năm năm. Kết quả: khi được phép dùng AI, họ hoàn thành công việc chậm hơn 19% so với khi không dùng, dù trước đó họ dự đoán AI sẽ giúp nhanh hơn 24%, và sau khi làm xong vẫn tin rằng mình đã nhanh hơn 20% [4][5]. Khoảng cách giữa cảm nhận và thực tế lớn đến mức đáng ngạc nhiên.

Số liệu này cần đọc thận trọng vì mẫu chỉ có 16 người và bối cảnh là mã nguồn mở phức tạp, không đại diện cho mọi loại công việc lập trình. Thực ra chính nhóm METR sau đó cập nhật rằng với một nhóm nhỏ hơn trong cùng cohort, đến đầu 2026 họ đo được mức tăng tốc 18%, cho thấy công cụ và kỹ năng dùng AI có thể đã cải thiện theo thời gian [6]. Cũng có thể mẫu quá nhỏ để kết luận chắc chắn theo bất kỳ hướng nào. Nhóm biên soạn báo cáo này chưa thống nhất nên tin số liệu nào hơn.

Nhìn xa hơn vòng lặp viết code, AI cũng đang len vào các khâu trước đó: viết đặc tả, sinh sơ đồ kiến trúc, review pull request tự động, viết tài liệu. Nhưng quy trình quản lý, cách chia task, cách review, gần như chưa thay đổi tương ứng ở phần lớn tổ chức. Đây có lẽ là khoảng trống lớn nhất hiện nay giữa năng lực công cụ và năng lực tổ chức.

### 2.2. Lập trình viên cần bổ sung kỹ năng nào để làm việc hiệu quả với AI?

Kỹ năng đọc code kỹ hơn viết code. Khi AI sinh ra phần lớn code, việc đọc và đánh giá nhanh xem đoạn code đó có đúng logic, có an toàn, có phù hợp kiến trúc hay không trở thành kỹ năng cốt lõi, quan trọng hơn cả tốc độ gõ phím.

Kỹ năng đặt câu hỏi và chia nhỏ vấn đề. Prompt tệ ra kết quả tệ, nhưng vấn đề sâu hơn là biết chia một task lớn thành các bước mà AI có thể xử lý đúng từng bước. Đó chính là context engineering ở mức cá nhân, áp dụng hàng ngày chứ không phải lý thuyết suông.

Kỹ năng giữ vững kiến thức nền tảng. Nếu chỉ dựa vào AI mà không hiểu thuật toán, cấu trúc dữ liệu, nguyên lý thiết kế hệ thống, người dùng sẽ không nhận ra khi nào AI sai. 20% người trả lời khảo sát Stack Overflow 2025 lo ngại rằng dùng AI khiến họ kém tự tin hơn trong việc tự giải quyết vấn đề [1]. Nhóm biên soạn báo cáo này cũng chưa thống nhất nên coi đây là rủi ro của từng cá nhân hay rủi ro mang tính hệ thống của cả ngành. Có lẽ là cả hai.

Cuối cùng là kỹ năng thẩm định rủi ro bảo mật và bản quyền. AI có thể gợi ý code chứa lỗ hổng bảo mật quen thuộc, hoặc vô tình tái tạo đoạn code gần giống dữ liệu huấn luyện đã thấy trước đó. Người review cuối cùng vẫn là con người. Trách nhiệm pháp lý cũng vậy.

### 2.3. Chọn một công cụ AI để phát triển phần mềm cho doanh nghiệp

Bảng so sánh dưới đây dựa trên các đánh giá công khai gần đây về ba công cụ, cần lưu ý đây là nhận định tổng hợp từ báo chí kỹ thuật chứ không phải tài liệu chính thức của từng hãng, nên có thể lệch theo thời gian khi các hãng cập nhật sản phẩm [9].

| Tiêu chí | GitHub Copilot | Cursor | Claude Code |
|---|---|---|---|
| Bảo mật / tuân thủ | SOC 2 Type II, có cam kết bồi thường nếu code sinh ra vướng khiếu nại bản quyền, kiểm soát truy cập gắn liền hệ sinh thái GitHub Enterprise | Có gói team và enterprise, tùy chọn zero data retention, nhưng năng lực logging và kiểm soát tuân thủ được đánh giá còn non hơn hai đối thủ | SOC 2 Type II, tuân thủ GDPR, có API doanh nghiệp cam kết không dùng code khách hàng để huấn luyện mô hình, hỗ trợ audit log, đang hướng tới HIPAA |
| Tích hợp | Sâu nhất với GitHub, Azure DevOps, VS Code, JetBrains, đã có mặt sẵn trong quy trình của rất nhiều tổ chức | IDE riêng dựa trên VS Code, tích hợp tốt trong trình soạn thảo nhưng ít gắn với hệ sinh thái quản trị doanh nghiệp sẵn có | CLI và SDK, dễ gắn vào pipeline tự động hóa và script, nhưng đòi hỏi đội ngũ tự xây quy trình tích hợp nhiều hơn |
| Chi phí chuyển đổi | Thấp nếu tổ chức đã dùng GitHub Enterprise, gần như chỉ cần bật tính năng | Trung bình, yêu cầu chuyển hẳn sang một IDE mới, ảnh hưởng thói quen làm việc hàng ngày | Trung bình đến cao với nhóm quen giao diện đồ họa, thấp với nhóm quen dòng lệnh và tự động hóa |
| Năng lực kỹ thuật (agentic) | Có Copilot Workspace dạng agent nhưng còn giới hạn hơn hai đối thủ | Agent Mode mạnh, phản hồi nhanh, phù hợp vòng lặp sửa code liên tục | Agentic mạnh nhất trong việc tự lập kế hoạch, đọc nhiều file, chạy lệnh nhiều bước mà không cần giám sát liên tục |

Nếu chỉ chọn một công cụ cho một doanh nghiệp bậc trung, không phải công ty AI-first cũng không phải tổ chức tài chính siêu nhạy cảm, tôi nghiêng về GitHub Copilot. Lý do chính không phải vì nó mạnh nhất về mặt agentic, thực ra Claude Code vượt trội hơn ở đó, mà vì chi phí chuyển đổi thấp và mức độ trưởng thành về quản trị doanh nghiệp như SSO, audit log, chính sách tổ chức hiện vẫn dẫn đầu [9]. Với một doanh nghiệp cần triển khai nhanh cho hàng trăm lập trình viên, quản lý bằng chính sách tập trung, và cần một câu trả lời rõ ràng cho bộ phận pháp chế, yếu tố này quyết định nhiều hơn năng lực agentic thuần túy.

Điều kiện sẽ làm lựa chọn này thay đổi: nếu doanh nghiệp thiên về đội ngũ kỹ thuật cao, làm nhiều tác vụ tự động hóa phức tạp qua dòng lệnh, cần agent tự chủ xử lý các luồng việc dài như tự động hóa refactor toàn bộ codebase hay tự viết và chạy test hàng loạt, Claude Code sẽ hợp lý hơn nhờ năng lực agentic và cam kết không dùng dữ liệu khách hàng để huấn luyện mô hình. Nếu ưu tiên hàng đầu là trải nghiệm chỉnh sửa code trực tiếp trong một IDE thiết kế riêng cho AI, và tổ chức chấp nhận đánh đổi một phần kiểm soát tập trung để đổi lấy tốc độ của đội nhỏ, Cursor là phương án đáng cân nhắc hơn. Câu trả lời không cố định. Nó phụ thuộc vào việc doanh nghiệp coi trọng kiểm soát tập trung hay năng lực agentic hơn, và đó là điều mỗi tổ chức phải tự cân nhắc dựa trên quy mô đội ngũ kỹ thuật hiện có.

## Tài liệu tham khảo

[1] Stack Overflow. "2025 Stack Overflow Developer Survey: AI." https://survey.stackoverflow.co/2025/ai

[2] Stack Overflow. "Stack Overflow's 2025 Developer Survey Reveals Trust in AI at an All Time Low." https://stackoverflow.co/company/press/archive/stack-overflow-2025-developer-survey/

[3] Google Cloud Blog. "Announcing the 2025 DORA Report." https://cloud.google.com/blog/products/ai-machine-learning/announcing-the-2025-dora-report

[4] METR. "Measuring the Impact of Early-2025 AI on Experienced Open-Source Developer Productivity." https://metr.org/blog/2025-07-10-early-2025-ai-experienced-os-dev-study/

[5] METR. arXiv:2507.09089. https://arxiv.org/abs/2507.09089

[6] METR. "We are Changing our Developer Productivity Experiment Design." https://metr.org/blog/2026-02-24-uplift-update/

[7] Anthropic. "Introducing the Model Context Protocol." https://www.anthropic.com/news/model-context-protocol

[8] Model Context Protocol Blog. "The 2026-07-28 Specification." https://blog.modelcontextprotocol.io/posts/2026-07-28/

[9] MintMCP Blog. "Claude Code vs Cursor vs Copilot: 2026 Security Comparison." https://www.mintmcp.com/blog/claude-code-cursor-vs-copilot
